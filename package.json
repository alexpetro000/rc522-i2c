{
  "name": "rc522-i2c",
  "description": "node.js module to access a rfid reader with rc522 chipset which is connected a raspberry pi via i2c",
  "keywords": [
    "rfid",
    "rc522",
    "i2c"
  ],
  "version": "0.1.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/epike/rc522-i2c.git"
  },
  "scripts": {
    "install": "(node-gyp rebuild) || (exit 0)",
    "preinstall": "(node-gyp configure) || (exit 0)",
    "clean": "((node-gyp clean) && (rm -rf node_modules)) || (exit 0)",
    "build-debug": "(node-gyp configure --debug && node-gyp rebuild --debug) || (exit 0)"
  },
  "main": "./main",
  "engines": {
    "node": "0.12.9"
  },
  "dependencies": {
  },
  "optionalDependencies": {
  },
  "gitHead": "",
  "bugs": {
    "url": "https://github.com/epike/rc522-i2c/issues"
  },
  "homepage": "",
  "directories": {}
}
